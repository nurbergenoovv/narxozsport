import React from 'react'
import { StyleSheet, Text } from 'react-native'

export default function HeaderTitleText({ text }) {
	return <Text className='font-bold text-2xl'>{text}</Text>
}

const styles = StyleSheet.create({})
